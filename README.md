# Database

### Configure database
1. Login to database using command

  `mysql -h 'hostname' -u 'username' -p`
2. Run library.sql

  `source 'path to file'`
3. Run libraryprocedures.sql

  `source 'path to file'`
